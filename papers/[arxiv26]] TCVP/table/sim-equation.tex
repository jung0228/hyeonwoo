\begin{equation}
\label{eq:modality_sims}
\begin{aligned}
s^v_i &= \mathrm{Sim}(u, C^v_i), \\
s^a_i &= \mathrm{Sim}(u, C^a_i).
\end{aligned}
\end{equation}
\noindent where $C^v_i$ and $C^a_i$ denote the caption sentences, and $\mathrm{Sim}(\cdot,\cdot)$ denotes cosine similarity between Qwen-3 embedding vectors